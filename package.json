{
  "name": "07_church-hub",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "setup:drive": "node --env-file-if-exists=.env.local scripts/setup-drive.ts",
    "setup:folders": "node --env-file-if-exists=.env.local scripts/setup-folders.ts",
    "seed:demo": "node --env-file-if-exists=.env.local scripts/seed-demo.ts",
    "test:drive": "node --env-file-if-exists=.env.local scripts/test-drive.ts",
    "redeploy": "node --env-file-if-exists=.env.local scripts/redeploy.ts",
    "typecheck": "next typegen && tsc --noEmit"
  },
  "dependencies": {
    "next": "16.3.3",
    "pretendard": "^1.3.9",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.3.3",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
